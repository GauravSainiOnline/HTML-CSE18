### HTML-CSE18

#Git Commands

git init
git add .
gil commit -m "Message"
git remote add origin "URL"
git push

To create a branch

git branch name
git branch -a (To check which branch is checking out )
git checkout name 


To Delete a branch
git branch -d name 
git push origin --d name
